/**
 * The version shown in the sidebar footer.
 *
 * Not read from package.json: the manifest version is the build artefact's
 * (`0.0.0`, never bumped), while this is the product version users quote back
 * in support threads. Bump it here when the release number changes.
 */
export const APP_VERSION = '1.0.0'
